services:
  graphhopper:
    image: israelhikingmap/graphhopper:latest
    ports:
      - "8989:8989"
    volumes:
      - ../data:/data
    command: >
      --input /data/hsinchu.osm.pbf
      --host 0.0.0.0
      --port 8989
